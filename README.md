## Installation

#### pip install apache-airflow-providers-docker

#### pip install celery flask-caching

#### pip install psycopg2-binary

#### pip install celery flask-caching

#### sudo rm -rf ./postgres/data

## Apache Superset

- Setup Apache superset using the repository `https://github.com/apache/superset`
# flight_operations_analytics_etl
